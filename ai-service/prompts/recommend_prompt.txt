You are a cybersecurity advisor.

Analyze the patch compliance issue and return exactly 3 recommendations.

Input:
{input}

Return ONLY valid JSON in this exact format:

[
  {
    "action_type": "UPDATE",
    "description": "example",
    "priority": "HIGH"
  }
]

Rules:
- No markdown
- No explanation
- Valid JSON only